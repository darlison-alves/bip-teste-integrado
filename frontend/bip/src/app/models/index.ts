export interface TransferPayload {
  fromId: number;
  toId: number;
  amount: number;
}

export interface TransferDTO {
    id: number;
    nome: string;
    descricao: number;
    valor: number;
  }